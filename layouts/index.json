{{- $items := slice -}}
{{- range .Site.RegularPages -}}
  {{- $items = $items | append (dict
    "title" .Title
    "url" .RelPermalink
    "section" .Section
    "summary" (.Summary | plainify | truncate 160)
  ) -}}
{{- end -}}
{{- $items | jsonify -}}